import AboutPage from "./AboutPage";

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About - Bryan Deckers",
};

const About = () => {
  return (
    <AboutPage />
  );
};

export default About;
