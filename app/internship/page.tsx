import InternshipPage from "./InternshipPage";

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Internship - Bryan Deckers",
};

const Intenship = () => {
  return <InternshipPage />;
};

export default Intenship;
